spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/rest_basic
    url: jdbc:mysql://localhost:3306/basic_logging
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    #    hibernate:
    #      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true

# swagger
springdoc.swagger-ui.path: /swagger-ui.html
